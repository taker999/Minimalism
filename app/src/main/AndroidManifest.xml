<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Minimalism"
        tools:targetApi="31">
        <activity
            android:name=".activities.PrintActivity"
            android:exported="false" />
        <activity
            android:name=".activities.FeedbackActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.SetEmailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.InputPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CreatePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PrivacyAndPolicyActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CreateOrEditNoteActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>